// Feynman's config shape is identical to Pi's — reuse Pi's builder, re-export under the Feynman name.
export { buildPiLocalConfig as buildFeynmanLocalConfig } from "@paperclipai/adapter-pi-local/ui";
